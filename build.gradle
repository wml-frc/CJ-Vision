plugins {
	id "cpp"
	id "edu.wpi.first.GradleRIO" version "2021.3.1"
	id "google-test-test-suite"
	id "groovy"
}

project.ext.set('projectRoot', ".")
apply from: project.file("$projectRoot/sharedGradle/mainBuilder.gradle")

wrapper {
	gradleVersion = '6.8'
}